[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo_mysql", "php82Extensions.curl", "php82Extensions.mbstring", "php82Extensions.json", "php82Extensions.session"]

[phases.build]
cmds = []

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
